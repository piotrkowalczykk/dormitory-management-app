import { Layout } from '../../components/Layout/Layout';
import classes from './EmailVerification.module.css';

export function EmailVerification(){
    return(
        <Layout>
            <div className={classes.root}>EmailVerification</div>
        </Layout>
    );
}