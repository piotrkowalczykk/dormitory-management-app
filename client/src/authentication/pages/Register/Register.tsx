import { Layout } from '../../components/Layout/Layout';
import classes from './Register.module.css';

export function Register(){
    return(
        <Layout>
            <div className={classes.root}>Register</div>
        </Layout>
    );
}