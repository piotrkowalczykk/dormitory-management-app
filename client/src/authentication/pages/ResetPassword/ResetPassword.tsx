import { Layout } from '../../components/Layout/Layout';
import classes from './ResetPassword.module.css';

export function ResetPassword(){
    return(
        <Layout>
            <div className={classes.root}>ResetPassword</div>
        </Layout>
    );
}